# No install.sh needed — none of these hit the setuptools<81/environs
# pinning issue that pymilvus/laion-clap hit in enrichment/api. A plain
# `pip install -r requirements.txt` works.
httpx==0.27.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
