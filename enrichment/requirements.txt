# Pinned, verified-working versions for the stage 3 enrichment venv on
# Python 3.12 / CPU-only. Install via `bash install.sh`, NOT
# `pip install -r requirements.txt` directly — laion-clap's own metadata
# pins numpy==1.23.5, which has no Python 3.12 wheel and fails to build
# from source under modern setuptools. install.sh installs laion-clap with
# --no-deps and pins its transitive deps to versions that actually work
# together on this Python/OS combo. This file exists as a version record.

torch==2.4.1+cpu
torchvision==0.19.1+cpu
laion-clap==1.1.6
torchlibrosa==0.1.0
librosa==0.11.0
soundfile==0.14.0
transformers==4.36.2
numpy==2.4.6
pymilvus==2.4.9
neo4j==5.28.1
psycopg2-binary==2.9.9
boto3==1.34.162
python-dotenv==1.0.1
setuptools==80.10.2
environs==15.1.0
